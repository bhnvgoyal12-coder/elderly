<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <!-- Package visibility for Android 11+ -->
    <queries>
        <!-- Samsung Apps -->
        <package android:name="com.samsung.android.messaging" />
        <package android:name="com.samsung.android.dialer" />
        <package android:name="com.sec.android.app.camera" />
        <package android:name="com.sec.android.gallery3d" />

        <!-- Google Apps (fallback) -->
        <package android:name="com.google.android.apps.messaging" />
        <package android:name="com.google.android.dialer" />
        <package android:name="com.google.android.GoogleCamera" />
        <package android:name="com.google.android.apps.photos" />

        <!-- WhatsApp -->
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />

        <!-- Intent for phone calls -->
        <intent>
            <action android:name="android.intent.action.DIAL" />
        </intent>

        <!-- Intent for querying all launchable apps -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ElderlyLauncher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:stateNotNeeded="true">

            <!-- Standard launcher entry -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- HOME intent - registers as a launcher -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AppDrawerActivity"
            android:exported="false"
            android:theme="@style/Theme.ElderlyLauncher" />
    </application>
</manifest>
